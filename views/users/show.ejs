<% include ../partials/header %>
   <style>
   body{
    background-color: #83d7fe;
}

   .a1{
   width:250px;
   min-height:250px;
    border:0px solid white;
    border-radius:50%; 
   }
   .a2{
   font
   }
.a3{
    background:#EEEEEE;
    margin-top:1.5em;
    width:100%;
    font-color:white;
}
 .a4{
 paddind:0.25;
  background-color:#EEEEEE;
  line-height:0.25em;
  font-color:#FFFFFF;
}
</style>

   <div class="row">
   <div class="col-md-3">
     <img src="../../uploads/<%=user.profileimage%>" class="img-circle a1">

   </div>
   <div class="col-md-9">

  
 <div class=a3>   <p><i><b>Name</b></i> :<%=user.name%></p></div>

   <div class=a3>  <p><i><b>Batchof</b></i> :<%=user.batchof%></p></div>

   <div class=a3><p><i><b>Relationship Status</b></i> :<%=user.status%></p></div>
    <div class=a3> <p><i><b>Bio</b></i> :<%=user.bio%></p></div>
    <div class=a3> <p><i><b>Instagram Id</b></i> :<%=user.insta%></p></div>
    <div>

                           <% if(currentUser && (currentUser.username===user.username)){ %>
                           
                        <form id="delete-form" action="/users/1/<%= user._id %>?_method=DELETE" method="POST">
                         <a class="btn btn-xs btn-warning" href="/users/1/<%= user._id %>/edit">Edit</a>
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>

                           
                        
                    <% }%>

    </div>


   </div>

   </div>
    <hr>
      
      <div class="well">
      <div class="a4">
      <h3><b>//ASK//SAY//CONFESS// </b></h3>
      <p>You can be anonymous or brave</p>
      </div>
      
                <div class="text-right">
                    <a class="btn btn-danger" href="/users/1/<%= user._id %>/comments/new">Say anything</a>
                    <a class="btn btn-warning" href="/users/1/<%= user._id %>/comments/new">Ask Anything</a>
                </div>



     <% user.comments.forEach(function(comment){ %>
                    <div class="row">
                        <div class="col-md-12">
                            <strong><%= comment.author.username %></strong>
                            
                       <div>
                           <%= comment.text %> 

                            <% if(currentUser && comment.author.id.equals(currentUser._id)){ %>
                                <form style="display: inline" class="pull-right" action="/users/1/<%=user._id%>/comments/<%=comment._id%>?_method=DELETE" method="POST">
                                    <button class="btn btn-xs btn-danger">DELETE</button>
                                    <a href="/users/1/<%=user._id%>/comments/<%=comment._id%>/edit" class="btn btn-xs btn-info">EDIT</a>
                                </form>
                            <% } %> 

                          </div>
                           </div> 
                          </div>
                          <% }) %>
                          </div>



<% include ../partials/footer %>